---
title: 手数料トークンコントラクトのデプロイ  
description: インセンティブ用の手数料トークン（ERC20）をデプロイする  
updated: 2024-06-09  
authors: [andyvargtz]  
icon: Terminal  
---
import { Steps, Step } from 'fumadocs-ui/components/steps';

リレイヤーにインセンティブを提供するためには、**どの ERC20 トークンを使用するかを指定** する必要があります。現在、ローカル環境にはERC20トークンが存在していないため、まず **手数料として利用するトークンをデプロイ** する必要があります。  

インセンティブの支払いは、**送信元のチェーンにデプロイされた ERC20 トークン** を使用して行います。例えば、ネイティブトークンをラップした ERC20 も使用可能ですが、ここでは **Avalanche-CLI に含まれているデモ用のシンプルな ERC20** を使います。  

それでは、この **FEE トークン** をデプロイし、一部のトークンをMint（発行）して、リレイヤーにインセンティブを提供できるようにしましょう。

<Steps>
<Step>

### 手数料トークンコントラクトのデプロイ  

以下のコマンドを実行すると、デモ用の ERC20 コントラクトがデプロイされます。

```bash
avalanche contract deploy erc20
```

このコマンドを実行すると、以下のような設定を求められます。次のように入力してください。  

```bash
✔ Local Network
✔ C-Chain

A private key is needed to pay for the contract deploy fees.
It will also be considered the owner address of the contract, beign able to call
the contract methods only available to owners.

✔ Use the private key of the Genesis Allocated address 0x8db97C7cEcE249c2b98bDC0226Cc4C2A57BF52FC

Which is the token symbol?
Token symbol: FEE
Which is the total token supply?
Token supply: 100000
Which address should receive the supply?

✔ Use the Genesis Allocated address 0x8db97C7cEcE249c2b98bDC0226Cc4C2A57BF52FC

Token Address: 0x768AF58E63775354938e9F3FEdB764F601c038b4

ERC20 Contract Successfully Deployed!
```

</Step>
<Step>

### デプロイした ERC20 トークンのアドレスを保存  

デプロイした手数料トークンのアドレスを環境変数に保存しておきましょう。  

```bash
export ERC20_fee_address=0x768AF58E63775354938e9F3FEdB764F601c038b4
```

</Step>
</Steps>